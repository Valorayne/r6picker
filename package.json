{
  "name": "r6picker",
  "version": "1.0.0",
  "dependencies": {
  },
  "scripts": {
    "start-backend": "cd backend && npm i && npm run start",
    "start-frontend": "cd frontend && npm i && npm run dev"
  },
  "engines": {
    "npm": ">=8.0.0 <9.0.0",
    "node": ">=16.0.0 <17.0.0"
  }
}
